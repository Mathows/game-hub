# Configs locais de ferramentas (uso pessoal — não versionar)
.claude/

# .NET / Visual Studio
bin/
obj/
*.user
.vs/
*.suo

# Build / publish
publish/
out/

# Configs sensíveis (segredos não vão para o git)
appsettings.*.Local.json
secrets.json
*.env

# Banco local
*.mdf
*.ldf

# Notas pessoais de aprendizado — vivem em REPOSITÓRIO PRIVADO separado.
# A pasta private/ tem o próprio .git e NUNCA entra no repo público.
private/
docs/APRENDIZADO.md
.private/
